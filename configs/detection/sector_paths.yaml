# Sector -> HLSP root override map for the per-sector BLS first pass.
#
# The default rule is /pdo/users/tehan/tglc-gpu-production/hlsp_s{NN:04d}/.
# Add an entry below only when a sector's HLSP tree lives somewhere else
# (e.g. a frozen reference run, a re-extraction, or a non-standard scratch).
#
# Example override:
#   56: /pdo/users/tehan/tglc-deep-catalogs/hlsp_s0056   # frozen reference

# (none yet — S56-S59 use the default GPU-production tree)
